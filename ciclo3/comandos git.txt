git init / iniciar git en repositorio

git config --global user.email tonyusb@gmail.com /se puede cambiar email por name para usar el username

git config --list 7 / lista de detalles

git status / archivos del repositorio estado 

git add PruebaGit.txt / habilitar para trabajar
git add --a / todos

git  commit -m "primera modificacion" / agrega la identificacion de la grabacion

git log / para ver las modificaciones

git branch nombre / crear rama de nombre

git log --oneline / log en una linea

git checkout -b master / cambio de rama

git branch / muestra los brazos
git branch -M main /antes de enviar al repositorio

echo "# aaa" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/oniotna2021/aaa.git
git push -u origin main

git  log --oneline --graph

git clone urldelainvitacion

git pull / se trae los cambios

git config user.name / ver el usuario

git config --global user.email tonyusb@gmail.com




